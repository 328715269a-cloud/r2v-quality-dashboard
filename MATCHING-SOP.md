# R2V 单镜头 / 多镜头人员匹配 SOP

## 统一原则

单镜头与多镜头使用同一套人员归属规则，仅按工作流字段隔离数据，不分别维护两套判断逻辑。

## 标注人匹配顺序

1. 已保存到云端的人工匹配优先级最高；重新上传 CSV 或 Excel 不得覆盖。
2. 自动匹配只认流水中的“标注”事件，即实际完成并提交的人员。
3. “标注派单、领取、待定”只表示任务曾分配，不代表实际生产，不得归责。
4. 派单后出现“超时释放、手动释放、转派”且没有本人“标注”事件时，该人员不得计入。
5. 同一轮有多人被派单时，以该轮质检结果之前最后一次“标注”提交人为责任人。
6. 历史首轮准确率以首次质检结果之前的实际提交人为准；二次修改提交不回写并覆盖历史首轮责任人。
7. 没有实际提交证据时显示“待匹配”，不得用派单人猜测。

## 质检人与验收匹配

1. 质检人优先取对应质检结果流水的操作人；流水缺失时再取质检反馈 Excel 的质检人列。
2. 验收错误归属到该验收轮之前最近一次有效“质检通过”的质检人。
3. 标注人仍取对应质检轮之前实际“标注”提交人，不从验收派单反推。

## 上传与更新

1. CSV 可以包含历史全量数据；事件按稳定事件键去重，已有事件不重复累计，新事件补充进入完整链路。
2. 当天导出的最新流水用于补充最新状态，但不能删除历史生产事件。
3. 质检反馈 Excel 的业务内容以最新上传记录为准；申诉、裁决、人工匹配、修改/提交状态、复盘类型等线上字段必须保留。
4. 单镜头数据只参与单镜头统计，多镜头数据只参与多镜头统计。

## 每次发布前自动检查

- 只有派单、没有标注提交：必须待匹配。
- 派单后释放、没有标注提交：必须待匹配。
- 多次派单、最终由另一人提交：必须归属实际提交人。
- 二次提交：首轮与后续轮次分别取各轮之前的实际提交人。
- 人工匹配：刷新、重传和重新部署后必须保持。
- 同一套测试同时覆盖单镜头和多镜头。
