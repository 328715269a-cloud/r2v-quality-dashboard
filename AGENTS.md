# 线上作业保护规则

用户于 2026-09-08 明确要求：**“后面不管改什么，不要影响当时在作业的。”**

本规则适用于此工作区所有项目的后续修改、数据修正及发布，是持续有效的用户约束。

- 优先保护正在作业的用户，包括答题、交卷、填写与提交复盘、上传和文件解析。不得造成服务中断、强制刷新、重新登录、未提交内容丢失或任务重置。
- 先在隔离环境完成开发和验证。发布必须兼容已打开的旧页面、现有会话和正在执行的请求；数据库与接口变更采用兼容的增量方式。
- 不得直接沿用“先停止现有服务，再启动新服务”的发布脚本。切换前准备并验证新版本，切换后保留旧版本处理在途请求及回退能力；不能只以健康检查成功认定切换安全。
- 并行运行新旧服务前，检查后台任务和启动逻辑，避免重置正在处理的上传、重复消费任务或覆盖他人刚保存的内容。切换后的旧页面仍应能正常保存和提交。
- 数据修正应精准限定到用户授权的记录及字段，保留成绩、答卷、复盘和历史信息。备份并记录修改，使用短事务及适当冲突检查，避免阻塞正常作业。
- 无法保证当前作业不受影响时，先完成隔离准备并推迟上线，明确说明阻碍；不得把已有上线授权理解为允许影响正在作业的人。
