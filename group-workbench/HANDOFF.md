# 正式新作业模式接手记录

## 2026-09-20：验收退回后的质检批量转交（prod8）

`20260920-prod8` 已于 2026-09-20 18:05（北京时间）正式上线，修复质检台“批量打回”不能处理验收退回待质检分流任务的问题。精确发布与验证证据见 `docs/releases/workbench-20260920-prod8.json`。

- 验收退回的任务批量转交标注时，使用既有 `acceptance_route` 事件并设置 `route:annotation`；普通待质检和待复检任务继续使用 `qc_fail`。不能把验收退回任务当作新一轮普通质检直接打回。
- 支持只选择验收退回任务，或与普通待质检任务混选；页面分别显示两类数量。验收退回转交只填写处理说明；混选时错误等级和标签只用于普通质检任务，说明用于全部所选任务。
- 每条任务重新检查身份、小组、当前状态和版本，整批生成一个事务。任一任务失效、过期或越权时整批不保存，并保留处理说明与勾选。提交期间避免重复提交或改变上下文；必须取消或完成当前表单后再发起其他批量结果。
- 沿用现有云端接口、角色权限、轮次与状态投影，不新增 API、不修改数据库，也不修改既有任务或历史记录。用户遇到的具体业务 TID、任务内容及身份不进入 Git。

业务源只改 `app.js`，`index.html` 更新为 prod8 资源引用。server/rules/workflow/reports/样式/共享协议保持原样。新增 `scripts/verify-workbench-qc-return.cjs` 直接提取实际 app 函数、结合既有服务端规则，用合成任务完成 8 类检查，包括纯验收退回、混选、错误标签、过期状态、权限、整批原子性和保存忙锁。

隔离实际浏览器复现旧页面错误后，验证新版纯验收退回 2 条与混选 2 条均保存成功；漏填普通质检标签和任务过期时整批拒绝，说明与勾选保留。静态发布前保持打开的 prod7 页面，在发布后原草稿不变，普通质检批量打回仍实际保存成功。正式网站只预览新表单，验收退回被正确识别且不要求 P 级，未提交真实任务。

发布保留全部旧资源及十个既有服务，新增不可变 prod8 资源并原子替换新模式入口。12 个公网文件逐一匹配，API 源、Nginx、原模式、两个 Demo 和全部容器身份/启动时间未变；不重启服务、不 reload Nginx、不读写生产数据库、不强制刷新或重新登录。当前用户业务版本可以正常推进，不把某个 revision 固定值作为保护条件。

## 2026-09-20：初始影片任务仅管理员导入（prod7）

`20260920-prod7` 已于 2026-09-20 17:35（北京时间）正式上线，精确发布证据见 `docs/releases/workbench-20260920-prod7.json`。业务变更只收拢初始影片/TID 导入权限：管理员继续导入，标注、质检和验收角色均不能新增导入任务。输入管理或小组密码不会提升其角色。

- 前端质检导航隐藏“任务导入”，进入页面、预览和提交均检查管理员身份。
- 服务端同时检查新增 tasks、非 handoff 的导入 batches、dispatch 事件；即使绕开页面、伪装批次或混入正常质检事务，非管理员仍收到 403，整个混合事务不写入。
- 已打开的旧质检页面若尚有导入草稿，提交时预期收到 403，并保留输入与选择；不要求旧质检导入继续成功。既有会话保留，不强制退出或刷新。
- 质检建/领质检包、质检通过/打回、返修分配、小组每日负责人/人数/总量设置，以及原有资料更正规则保持不变。已导入任务继续正常流转；本次不删除任务或修改历史。

实际业务源改动仅为 `app.js` 权限与导航、`rules.cjs` 的服务器导入边界，以及 `index.html` 的版本引用。server、状态投影、共享协议、导出统计、样式和数据库结构不改动。发布先准备新兼容 API，再平滑切换新模式路由并等待旧请求排空；原有九个服务、旧静态资源和唯一备份任务全部保留。

固定生产 Node 22.23.2 镜像的独立临时数据库已通过管理员导入专项 8 项检查、direct-acceptance 全流程/双进程 WAL 并发和 200 条任务/2000 条校验边界回归。效率统计 14 类检查与验收导出 37 条合成事件回归通过。新专项使用隔离假 PIN，源码与公开证据不写入生产凭据。

隔离环境中的同一旧质检页面与原会话已验证：新增导入收到 403，草稿保留；普通质检包保存成功，已提交请求的幂等重试保持。上线后 12 个公网文件精确匹配，质检角色只读登录与共享连接正常，导航为小组总览/质检台/全部任务，无任务导入；本组锁定、质检建包、审核与返修分配入口仍可用。发布和核验未提交任何生产任务，业务 revision 保持 14。

## 2026-09-20：小组人均日效率与个人每日产出（prod6）

`20260920-prod6` 已于 2026-09-20 16:52（北京时间）正式上线。此轮仅调整正式新模式前端与导出，API、数据库、任务流转、Nginx 和现有服务均保持不变。发布精确散列与核验结果见 `docs/releases/workbench-20260920-prod6.json`。

### 统计口径

- 小组实际完成量来自日期区间内有效的标注 `submit` 记录，不使用组长手填总量 `total` 作为产出。同一 TID 在同一天重复提交只计一条，跨天提交则各天分别计一条；首次完成和返修完成均按实际提交日期计入。
- 单天人均日效率 = 当天实际完成 TID ÷ 当天在班人数。多天人均日效率 = 各天实际完成量合计 ÷ 各天在班人数合计，后者单位为人天；不能用期间不重复姓名数作分母，也不能把每天的人均效率简单平均。
- 在班人数沿用自动识别及手动调整后的每日值；半天对应的 0.5 人天只用于小组人均分母，不折算某个人的实际完成条数。
- 有实际完成但当日人数为零时，效率返回 `null` 并标记需核对人数；不能显示为零或无穷大。区间包含这类异常日期时保留异常提示，不能用其他日期的人数掩盖。
- 个人每日明细按实际提交人、实际日期统计；请假代修记到本次实际完成人，不回记原标注人。同人同日同 TID 去重，跨天分别计数。原始提交次数及历史流水继续保留，去重展示不删除事件。

### 页面、导出与验证

小组总览增加实际完成量、人天及人均日效率，个人每日产出提供对应明细；两者跟随日期区间、镜头模块和小组权限。导出增加个人每日表，并保留原任务、影片、小组、每日、建包和操作历史统计。

`scripts/verify-workbench-efficiency.cjs` 的 14 类统计检查全部通过，覆盖去重、人天、代修归属、零人数异常、日期/小组边界和不可变流水；`scripts/verify-direct-acceptance-reports.cjs` 的 37 条合成事件验证及本地启动器 9 项检查通过。默认办公浏览器中的实际界面、区间切换、导出、权限范围和宽表横向滚动验证通过；发布后 12 个公网文件逐一比对一致。

历史 `scripts/verify-workbench-reports.cjs` 在 `qcOperator` 对 `acceptance_pass` 的旧断言上存在失败，在本轮起点 `d222657` 也可复现。本次没有修改 `qcOperator`，不将该历史失败宣称为已修复，也不为通过它而改变效率或验收业务语义。

发布只新增 prod6 不可变静态资源并原子替换新模式入口。API / 数据库 / Nginx 和现有服务保持原状，旧资源和已打开页面继续可用；本轮工具不调用生产 API、不读取真实任务或启停服务。

Git 中六个源资源使用 `?v=20260920-prod6` 开发引用，线上使用对应 `.20260920-prod6` 不可变文件名。未改动的 data-io/shared-store/workflow 校验仅存在 Windows 换行差异后保留线上原字节，相关散列在公开发布证据中说明。

## 2026-09-20：验收直接流转（prod5）

已于 2026-09-20 16:36（北京时间）正式上线，版本 `20260920-prod5`。发布状态和精确散列见 `docs/releases/workbench-20260920-prod5.json`。此记录与源码属于正式共享新模式 `/group-workbench/`，根站点的当前作业模式、两个 Demo 和全部既有服务保持不变。

16:39 追加 `20260920-prod5a` 布局补丁：验收待办改为全宽，隐藏空的单条处理侧栏，小屏允许表格横向滚动。只新增 CSS 资源并切换 index 引用，API 和 JavaScript 仍为 prod5，原 prod5 CSS 保留。

此次从线上 WorkBuddy 已发布的最新页面取源，再修复前后端流转不一致。保留原有页面简化、标注“认领 TID / 提交完成”、人员与小组范围等修改；没有用旧 prod4 前端覆盖这些修改。

### 验收规则

- 质检通过后直接进入验收台，不再建验收包。质检自行返修完成后也直接重入验收，验收轮次加一。
- 验收台汇总本镜头模块内流入的待验收任务，支持逐条勾选、当前筛选结果全选，以及批量通过、不通过和拒绝。已有 TID 粘贴定位与校验保留。
- 验收通过结束作业；不通过退回质检处理。质检可以自行修改，也可转交标注；转标注后必须再次通过质检。
- 验收拒绝为终态，记录拒绝阶段、原因和人员，不退回标注或质检；批次任一已处理任务冲突时整批不保存。
- 以前已建、未领取或待建验收包的记录仍保留并兼容读取。旧页面的精确建包/领取动作可继续保存为历史信息，不创建额外验收轮次，也不能复活终态。
- WorkBuddy 既有拒绝 marker 与新的规范事件同时兼容。服务器重新验证角色、组别、轮次、任务版本、原因、当前状态，拒绝过期或跨权限写入。

### 文件与验证

`app.js` 和 `index.html` 负责验收汇总与批量选择；`workflow.js` 是前后端共用的状态投影；`rules.cjs` 是服务端权限与状态规则；`reports.js` 保留验收拒绝和返修的完整统计与导出。

运行 `node workbench-api/verify-direct-acceptance.cjs`，使用独立临时 SQLite，验证三种验收结果、批量原子性、旧页面/原会话兼容、两个独立进程共享 WAL、持久恢复、在线备份、导出和权限边界。生产数据库不参与测试。

运行 `node scripts/verify-direct-acceptance-reports.cjs`，检查所有导出表的列键唯一、验收拒绝与打回分别计数、影片闭环和完整历史保留。本轮同时移除了影片导出中重复追加的三个统计列；这些字段仍保留在汇总列中。固定生产 Node 22.23.2 镜像的隔离验证、旧页未提交草稿与会话连续性、最终上线后 13 个公网文件精确比对均通过，发布过程未创建或修改任何真实任务。

线上使用 `app.20260920-prod5.js` 等不可变资源名及 `styles.20260920-prod5a.css`；Git 中 `index.html` 使用 `app.js?v=20260920-prod5` 和 `styles.css?v=20260920-prod5a` 等开发引用，以便现有本地启动器直接读取七个源文件。打包只转换这六个资源引用，不改变其余 HTML 或 JS/CSS 内容。两种 HTML 的散列差异是预期的。

发布采用新的独立 API 实例，保留原实例、原会话、原静态资源及唯一备份任务，仅平滑切换新模式 API 路由并原子替换新模式入口。不得把回退 HTML 等同于回退数据库或旧 API；新事件落库后必须保留兼容后端。

生产私钥、环境文件、密码、真实任务及完整内部运维清单不进入 Git。公共发布证据只包含源码散列、结果和发布保护断言。
